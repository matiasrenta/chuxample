<li>
  <div class="actions-hover btn-group" role="group">
    <a type="button" href="<%= trackable_history(activity) %>" class="btn btn-sm btn-default" rel="tooltip" data-placement="top" title="Historial del elemento">
      <i class="fa fa-history"></i>
    </a>
  </div>
  <div class="smart-timeline-icon">
    <%= owner_avatar(activity) %>
  </div>
  <div class="smart-timeline-time">
    <small><%= time_ago_in_words(activity.created_at) %></small>
  </div>
  <div class="smart-timeline-content">
    <p>
      <%= user_label(activity) %>
      Generó una solicitud de <%= activity.parameters[:status] %> del proyecto <%= trackable_label(activity) %> (sujeto a aprobación/rechazo por un revisor).
    </p>
  </div>
</li>
