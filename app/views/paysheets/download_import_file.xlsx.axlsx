wb = xlsx_package.workbook
wb.add_worksheet(name: t("activerecord.models.paysheet.other")) do |sheet|
  sheet.add_row (Paysheet.column_names - %W(id created_at updated_at project_activity_nomina_id job_title_id ascription_id)).map { |c| I18n.t("simple_form.labels.defaults.#{c.split('_id').first}") }
end
